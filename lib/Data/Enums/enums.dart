enum VendrButtonType {
  outlineButton,
  filledButton,
  filledSelectButton,
  filledButtonRounded,
  doubleButton,
  checkButton,
}

enum ScreenSize {
  small,
  normal,
  large,
  extraLarge,
}

enum CustomTextFieldType {
  generalTextField,
  sqrTextField,
}
